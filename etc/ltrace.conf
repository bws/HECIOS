int MPI_Abort(addr, int);
int MPI_Accumulate(addr, int, addr, int, addr, int, addr,  addr, addr);
int MPI_Allgather(addr , int, addr, addr, int, addr, addr);
int MPI_Allgatherv(addr , int, addr, addr, addr, addr, addr, addr);
int MPI_Alloc_mem(addr, addr, addr);
int MPI_Allreduce(addr , addr, int, addr, addr, addr);
int MPI_Alltoall(addr , int, addr, addr, int, addr, addr);
int MPI_Alltoallv(addr , addr, addr, addr, addr, addr, addr, addr, addr);
int MPI_Alltoallw(addr, addr, addr, addr, addr, addr, addr, addr, addr);
int MPI_Attr_delete(addr, int);
int MPI_Attr_get(addr, int, addr, addr);
int MPI_Attr_put(addr, int, addr);
int MPI_Barrier(addr);
int MPI_Barrier(addr);
int MPI_Bcast(addr, int, addr, int, addr);
int MPI_Bsend(addr, int, addr, int, int, addr);
int MPI_Bsend_init(addr, int, addr, int,int, addr, addr);
int MPI_Buffer_attach( addr, int);
int MPI_Buffer_detach( addr, addr);
int MPI_Cancel(addr);
int MPI_Cart_coords(addr, int, int, addr);
int MPI_Cart_create(addr, int, addr, addr, int, addr);
int MPI_Cart_get(addr, int, addr, addr, addr);
int MPI_Cart_map(addr, int, addr, addr, addr);
int MPI_Cart_rank(addr, addr, addr);
int MPI_Cart_shift(addr, int, int, addr, addr);
int MPI_Cart_sub(addr, addr, addr);
int MPI_Cartdim_get(addr, addr);
int MPI_Comm_compare(addr, addr, addr);
int MPI_Comm_create(addr, addr, addr);
int MPI_Comm_dup(addr, addr);
int MPI_Comm_free(addr);
int MPI_Comm_get_name(addr, string, addr);
int MPI_Comm_group(addr, addr);
int MPI_Comm_rank(addr, addr);
int MPI_Comm_remote_group(addr, addr);
int MPI_Comm_remote_size(addr, addr);
int MPI_Comm_set_name(addr, string);
int MPI_Comm_size(addr, addr);
int MPI_Comm_split(addr, int, int, addr);
int MPI_Comm_test_inter(addr, addr);
int MPI_Dims_create(int, int, addr);
int MPI_Errhandler_create(addr, addr);
int MPI_Errhandler_free(addr);
int MPI_Errhandler_get(addr, addr);
int MPI_Errhandler_set(addr, addr);
int MPI_Error_class(int, addr);
int MPI_Error_string(int, string, addr);
int MPI_File_c2f(addr);
int MPI_File_close(addr);
int MPI_File_delete(string, addr);
int MPI_File_f2c(addr);
int MPI_File_get_amode(addr, addr);
int MPI_File_get_atomicity(addr, addr);
int MPI_File_get_byte_offset(addr, addr, addr);
int MPI_File_get_errhandler(addr, addr);
int MPI_File_get_group(addr, addr);
int MPI_File_get_info(addr, addr);
int MPI_File_get_position(addr, addr);
int MPI_File_get_position_shared(addr, addr);
int MPI_File_get_size(addr, addr);
int MPI_File_get_type_extent(addr, addr, addr);
int MPI_File_get_view(addr, addr, addr, addr, string);
int MPI_File_iread(addr, addr, int, addr, addr); 
int MPI_File_iread_at(addr, addr, addr, int, addr, addr);
int MPI_File_iread_shared(addr, addr, int, addr, addr);
int MPI_File_iwrite(addr, addr, int, addr, addr);
int MPI_File_iwrite_at(addr, addr, addr, int, addr, addr);
int MPI_File_iwrite_shared(addr, addr, int, addr, addr);
int MPI_File_open(addr, string, int, addr, addr);
int MPI_File_preallocate(addr, long);
int MPI_File_read(addr, addr, long, addr, addr); 
int MPI_File_read_all(addr, addr, long, addr, addr); 
int MPI_File_read_all_begin(addr, addr, long, addr);
int MPI_File_read_all_end(addr, addr, addr);
int MPI_File_read_at(addr, long, addr, long, addr, addr);
int MPI_File_read_at_all(addr, long, addr, long, addr, addr);
int MPI_File_read_at_all_begin(addr, long, addr, long, addr);
int MPI_File_read_at_all_end(addr, addr, addr);
int MPI_File_read_ordered(addr, addr, long, addr, addr);
int MPI_File_read_ordered_begin(addr, addr, long, addr);
int MPI_File_read_ordered_end(addr, addr, addr);
int MPI_File_read_shared(addr, addr, long, addr, addr);
int MPI_File_seek(addr, long, int);
int MPI_File_seek_shared(addr, long, int);
int MPI_File_set_atomicity(addr, int);
int MPI_File_set_errhandler( addr, addr);
int MPI_File_set_errhandler(addr, addr);
int MPI_File_set_info(addr, addr);
int MPI_File_set_size(addr, long);
int MPI_File_set_view(addr, long, addr, addr, string, addr);
int MPI_File_sync(addr);
int MPI_File_write(addr, addr, int, addr, addr);
int MPI_File_write_all(addr, addr, int, addr, addr);
int MPI_File_write_all_begin(addr, addr, int, addr);
int MPI_File_write_all_end(addr, addr, addr);
int MPI_File_write_at(addr, long, addr, int, addr, addr);
int MPI_File_write_at_all(addr, long, addr, int, addr, addr);
int MPI_File_write_at_all_begin(addr, long, addr, int, addr);
int MPI_File_write_at_all_end(addr, addr, addr);
int MPI_File_write_ordered(addr, addr, int, addr, addr);
int MPI_File_write_ordered_begin(addr, addr, int, addr);
int MPI_File_write_ordered_end(addr, addr, addr);
int MPI_File_write_shared(addr, addr, int, addr, addr);
int MPI_Finalized(addr);
int MPI_Finalize(void);
int MPI_Free_mem(addr);
int MPI_Gather(addr , int, addr, addr, int, addr, int, addr); 
int MPI_Gatherv(addr , int, addr, addr, addr, addr, addr, int, addr); 
int MPI_Get_address(addr, addr);
int MPI_Get(addr, int, addr, int, addr, int, addr, addr);
int MPI_Get_count(addr, addr, addr);
int MPI_Get_elements(addr, addr, addr);
int MPI_Get_processor_name(string, addr);
int MPI_Get_version(addr, addr);
int MPI_Graph_create(addr, int, addr, addr, int, addr);
int MPI_Graphdims_get(addr, addr, addr);
int MPI_Graph_get(addr, int, int, addr, addr);
int MPI_Graph_map(addr, int, addr, addr, addr);
int MPI_Graph_neighbors(addr, int, int, addr);
int MPI_Graph_neighbors_count(addr, int, addr);
int MPI_Grequest_complete(addr);
int MPI_Grequest_start(addr, addr, addr, addr, addr);
int MPI_Group_c2f(addr);
int MPI_Group_compare(addr, addr, addr);
int MPI_Group_difference(addr, addr, addr);
int MPI_Group_excl(addr, int, addr, addr);
int MPI_Group_f2c(addr);
int MPI_Group_free(addr);
int MPI_Group_incl(addr, int, addr, addr);
int MPI_Group_intersection(addr, addr, addr);
int MPI_Group_range_excl(addr, int, addr, addr);
int MPI_Group_range_incl(addr, int, addr, addr);
int MPI_Group_rank(addr, addr);
int MPI_Group_size(addr, addr);
int MPI_Group_translate_ranks(addr, int, addr, addr, addr);
int MPI_Group_union(addr, addr, addr);
int MPI_Ibsend(addr, int, addr, int, int, addr, addr);
int MPI_Info_c2f(addr);
int MPI_Info_c2f(addr);
int MPI_Info_create(addr);
int MPI_Info_create(addr);
int MPI_Info_delete(addr, string);
int MPI_Info_delete(addr, string);
int MPI_Info_dup(addr, addr);
int MPI_Info_dup(addr, addr);
int MPI_Info_f2c(addr);
int MPI_Info_f2c(addr);
int MPI_Info_free(addr);
int MPI_Info_free(addr);
int MPI_Info_get(addr, string, int, string, addr);
int MPI_Info_get(addr, string, int, string, addr);
int MPI_Info_get_nkeys(addr, addr);
int MPI_Info_get_nkeys(addr, addr);
int MPI_Info_get_nthkey(addr, int, string);
int MPI_Info_get_nthkey(addr, int, string);
int MPI_Info_get_valuelen(addr, string, addr, addr);
int MPI_Info_get_valuelen(addr, string, addr, addr);
int MPI_Info_set(addr, string, string);
int MPI_Info_set(addr, string, string);
int MPI_Init(addr, addr);
int MPI_Initialized(addr);
int MPI_Init_thread(addr, addr, int, addr);
int MPI_Intercomm_create(addr, int, addr, int, int, addr );
int MPI_Intercomm_merge(addr, int, addr);
int MPI_Iprobe(int, int, addr, addr, addr);
int MPI_Irecv(addr, int, addr, int, int, addr, addr);
int MPI_Irsend(addr, int, addr, int, int, addr, addr);
int MPI_Isend(addr, int, addr, int, int, addr, addr);
int MPI_Issend(addr, int, addr, int, int, addr, addr);
int MPI_Is_thread_main(addr);
int MPI_Keyval_create(addr, addr, addr, addr);
int MPI_Keyval_free(addr);
int MPI_Lookup_name(string, addr, string);
int MPI_Op_c2f(addr);
int MPI_Op_create(addr, int, addr);
int MPI_Open_port(addr, string);
int MPI_Op_f2c(addr);
int MPI_Op_free( addr);
int MPIO_Request_c2f(addr);
int MPIO_Request_f2c(addr);
int MPIO_Test(addr, addr, addr);
int MPIO_Testall(int, addr, addr, addr);
int MPIO_Testany(int, addr, addr, addr, addr);
int MPIO_Testsome(int, addr, addr, addr, addr);
int MPIO_Wait(addr, addr);
int MPIO_Waitall(int, addr, addr);
int MPIO_Waitany(int, addr, addr, addr);
int MPIO_Waitsome(int, addr, addr, addr, addr);
int MPI_Pack(addr, int, addr, addr, int, addr,  addr);
int MPI_Pack_external_size(string, int, addr, addr); 
int MPI_Pack_external(string, addr, int, addr, addr, addr, addr); 
int MPI_Pack_size(int, addr, addr, addr);
int MPI_Probe(int, int, addr, addr);
int MPI_Publish_name(string, addr, string);
int MPI_Put(addr, int, addr, int, addr, int, addr, addr);
int MPI_Query_thread(addr);
int MPI_Recv(addr, int, addr, int, int, addr, addr);
int MPI_Recv_init(addr, int, addr, int,int, addr, addr);
int MPI_Reduce(addr , addr, int, addr, addr, int, addr);
int MPI_Reduce_scatter(addr , addr, addr, addr, addr, addr);
int MPI_Register_datarep(string, addr, addr, addr, addr);
int MPI_Request_c2f(addr);
int MPI_Request_f2c(addr);
int MPI_Request_free(addr);
int MPI_Request_get_status(addr, addr, addr);
int MPI_Rsend(addr, int, addr, int, int, addr);
int MPI_Rsend_init(addr, int, addr, int,int, addr, addr);
int MPI_Scan(addr , addr, int, addr, addr, addr);
int MPI_Scatter(addr , int, addr, addr, int, addr, int, addr);
int MPI_Scatterv(addr , addr, addr,  addr, addr, int, addr, int, addr);
int MPI_Send(addr, int, addr, int, int, addr);
int MPI_Send_init(addr, int, addr, int, int, addr, addr);
int MPI_Sendrecv(addr, int, addr,int, int, addr, int, addr, int, int, addr, addr);
int MPI_Sendrecv_replace(addr, int, addr, int, int, int, int, addr, addr);
int MPI_Ssend(addr, int, addr, int, int, addr);
int MPI_Ssend_init(addr, int, addr, int,int, addr, addr);
int MPI_Start(addr);
int MPI_Startall(int, addr);
int MPI_Status_c2f(addr, addr);
int MPI_Status_f2c(addr, addr);
int MPI_Status_set_cancelled(addr, int);
int MPI_Status_set_elements(addr, addr, int);
int MPI_Test(addr, addr, addr);
int MPI_Testall(int, addr, addr, addr);
int MPI_Testany(int, addr, addr, addr, addr);
int MPI_Test_cancelled(addr, addr);
int MPI_Testsome(int, addr, addr, addr, addr);
int MPI_Topo_test(addr, addr);
int MPI_Type_c2f(addr);
int MPI_Type_commit(addr);
int MPI_Type_contiguous(int, addr, addr);
int MPI_Type_create_darray(int, int, int, addr, addr, addr, addr, int, addr, addr);
int MPI_Type_create_darray(int, int, int, addr, addr, addr, addr, int, addr, addr);
int MPI_Type_create_f90_complex( int, int, addr );
int MPI_Type_create_f90_integer( int, addr );
int MPI_Type_create_f90_real( int, int, addr );
int MPI_Type_create_hindexed(int, addr, addr, addr, addr);
int MPI_Type_create_hvector(int, int, addr, addr, addr);
int MPI_Type_create_indexed_block(int, int, addr, addr, addr);
int MPI_Type_create_keyval(addr, addr, addr, addr);
int MPI_Type_create_resized(addr, addr, addr, addr);
int MPI_Type_create_struct(int, addr, addr, addr, addr);
int MPI_Type_create_subarray(int, addr, addr, addr, int, addr, addr);
int MPI_Type_create_subarray(int, addr, addr, addr, int, addr, addr);
int MPI_Type_delete_attr(addr, int);
int MPI_Type_dup(addr, addr);
int MPI_Type_extent(addr, addr);
int MPI_Type_f2c(addr);
int MPI_Type_free(addr);
int MPI_Type_free_keyval(addr);
int MPI_Type_get_attr(addr, int, addr, addr);
int MPI_Type_get_contents(addr, int, int, int, addr, addr, addr);
int MPI_Type_get_envelope(addr, addr, addr, addr, addr);
int MPI_Type_get_extent(addr, addr, addr);
int MPI_Type_get_name(addr, string, addr);
int MPI_Type_get_true_extent(addr, addr, addr);
int MPI_Type_hindexed(int, addr, addr, addr, addr);
int MPI_Type_hvector(int, int, addr, addr, addr);
int MPI_Type_indexed(int, addr, addr, addr, addr);
int MPI_Type_lb(addr, addr);
int MPI_Type_match_size( int, int, addr);
int MPI_Type_set_attr(addr, int, addr);
int MPI_Type_set_name(addr, string);
int MPI_Type_size(addr, addr);
int MPI_Type_struct(int, addr, addr, addr, addr);
int MPI_Type_ub(addr, addr);
int MPI_Type_vector(int, int, int, addr, addr);
int MPI_Unpack(addr, int, addr, addr, int, addr, addr);
int MPI_Unpack_external(string, addr, addr, addr, addr, int, addr); 
int MPI_Unpublish_name(string, addr, string);
int MPI_Wait(addr, addr);
int MPI_Waitall(int, addr, addr);
int MPI_Waitany(int, addr, addr, addr);
int MPI_Waitsome(int, addr, addr, addr, addr);
int MPI_Win_c2f(addr);
int MPI_Win_call_errhandler(addr, int);
int MPI_Win_complete(addr);
int MPI_Win_create(addr, addr, int, addr, addr, addr);
int MPI_Win_create_errhandler(addr, addr);
int MPI_Win_create_keyval(addr, addr, addr, addr);
int MPI_Win_delete_attr(addr, int);
int MPI_Win_f2c(addr);
int MPI_Win_fence(int, addr);
int MPI_Win_free(addr);
int MPI_Win_free_keyval(addr);
int MPI_Win_get_attr(addr, int, addr, addr);
int MPI_Win_get_errhandler(addr, addr);
int MPI_Win_get_group(addr, addr);
int MPI_Win_get_name(addr, string, addr);
int MPI_Win_lock(int, int, int, addr);
int MPI_Win_post(addr, int, addr);
int MPI_Win_set_attr(addr, int, addr);
int MPI_Win_set_errhandler(addr, addr);
int MPI_Win_set_name(addr, string);
int MPI_Win_start(addr, int, addr);
int MPI_Win_test(addr, addr);
int MPI_Win_unlock(int, addr);
int MPI_Win_wait(addr);
addr MPI_Wtick(void);
addr MPI_Wtime(void);
